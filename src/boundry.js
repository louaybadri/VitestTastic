// La fonction isPriceInRange(price, min, max) vérifie si un prix donné se situe dans une plage donnée.

export default function isPriceInRange(price, min, max) {
    return price >= min && price <= max;
}

